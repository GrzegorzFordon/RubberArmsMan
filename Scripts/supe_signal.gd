class_name SupeSignal
extends Node3D

@export var marker_3d: Marker3D
@export var shader_mesh: MeshInstance3D

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
	#shader_mesh.global_position.x = marker_3d.global_position.x
	#shader_mesh.global_position.z = marker_3d.global_position.z
